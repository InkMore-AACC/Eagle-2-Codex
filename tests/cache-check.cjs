const fs=require('fs'),vm=require('vm'),assert=require('assert');const s=fs.readFileSync('plugin/web/index.html','utf8').split('<script>')[1].split('</script>')[0];let calls=0;const c={display:{name:true,ext:true,tags:true,prompt:true},measured:new Map(),metadata:i=>i,measureBox:{style:{},replaceChildren(){calls++},firstChild:{getBoundingClientRect:()=>({height:123})}}};vm.createContext(c);vm.runInContext(s.slice(s.indexOf('function measuredMeta'),s.indexOf('async function edit')),c);
for(let n=0;n<3002;n++)c.measuredMeta({name:String(n),ext:'jpg',tags:['a'],annotation:'text'},220);
assert.equal(c.measured.size,3000);const prior=calls;c.measuredMeta({name:'3001',ext:'jpg',tags:['a'],annotation:'text'},220);assert.equal(calls,prior);
c.measuredMeta({name:'3001',ext:'jpg',tags:['a'],annotation:'changed'},220);assert.equal(calls,prior+1);console.log('PASS: bounded cache, recent entry reused, edited content remeasured');
